<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permission for AI and URL links -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Tamid_beshvil_hatalmid_try1">

        <!-- 1. The Startup Screen (Login/Register) -->
        <activity
            android:name=".SignInUp_Activity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".Home_Page"
            android:exported="false" />

        <activity
            android:name=".AI_page"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />
        <activity
            android:name=".QuestionBillActivity"
            android:exported="false" />

        <activity
            android:name=".AI_Solve_Photo_Activity"
            android:exported="false" />

        <activity
            android:name=".PersonalInfo_Page"
            android:exported="false" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />

    </application>

</manifest>